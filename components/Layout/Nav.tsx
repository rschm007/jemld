import { DefaultProps } from "@/@types";
import Link from "next/link";

export const Nav = ({
    className = "",
    id
}: DefaultProps) => {

    return (
        <nav>
            <Link href="" />
        </nav>
    )
}